device: "cuda:0"
output_dir: "runs/predict"
save_vis: false

models:
  detector:
    weights: "weights/yolo_flake.pt" 
    conf_thresh: 0.25
    iou_thresh: 0.45
  
  classifier:
    weights: "weights/flake_classifier.pth"
    num_materials: 4
    material_dim: 64
    freeze_cnn: false